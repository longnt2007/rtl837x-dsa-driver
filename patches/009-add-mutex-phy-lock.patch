--- a/src/rtl837x.h
+++ b/src/rtl837x.h
@@ -93,6 +93,7 @@ struct rtl837x_priv {
 	struct regmap		*map;
 	struct mutex		map_lock;
 	struct mutex		ita_lock;
+	struct mutex 		phy_lock;
 	struct regmap		*map_8224;
 	struct mutex		map_8224_lock;
 	int			mdio_addr;
--- a/src/rtl837x_common.c
+++ b/src/rtl837x_common.c
@@ -245,7 +245,8 @@ int rtl837x_reg_bits_write(struct rtl837
     return regmap_update_bits(priv->map, reg, mask, (val << __ffs(mask)) & mask);
 }
 
-int rtl837x_phy_read_c45(struct rtl837x_priv *priv, int phy, int devad, int regnum, u16 *pval)
+static int __rtl837x_phy_read_c45(struct rtl837x_priv *priv, int phy,
+                   int devad, int regnum, u16 *pval)
 {
 	int ret;
     u32 tmp;
@@ -279,6 +280,21 @@ int rtl837x_phy_read_c45(struct rtl837x_
 	return 0;
 }
 
+int rtl837x_phy_read_c45(struct rtl837x_priv *priv, int phy,
+             int devad, int regnum, u16 *pval)
+{
+    int ret;
+
+    if (phy < 0 || phy >= priv->num_ports)
+        return -EINVAL;
+
+    mutex_lock(&priv->phy_lock);
+    ret = __rtl837x_phy_read_c45(priv, phy, devad, regnum, pval);
+    mutex_unlock(&priv->phy_lock);
+
+    return ret;
+}
+
 int rtl837x_phy_read_ocp(struct rtl837x_priv *priv, u16 phy, int regnum, u16 *pval)
 {
 	int ret;
@@ -339,7 +355,8 @@ int rtl837x_phy_write_c22(struct rtl837x
 	return rtl837x_phy_write_ocp(priv, phy, 0xA400 + (regnum * 2), val);
 }
 
-int rtl837x_phys_write_c45(struct rtl837x_priv *priv, u16 phy_mask, int devad, int regnum, u16 val)
+static int __rtl837x_phys_write_c45(struct rtl837x_priv *priv, u16 phy_mask,
+                     int devad, int regnum, u16 val)
 {
 	int ret;
     u32 tmp;
@@ -372,9 +389,25 @@ int rtl837x_phys_write_c45(struct rtl837
     return 0;
 }
 
-int rtl837x_phy_write_c45(struct rtl837x_priv *priv, int phy, int devad, int regnum, u16 val)
+int rtl837x_phys_write_c45(struct rtl837x_priv *priv, u16 phy_mask,
+                int devad, int regnum, u16 val)
+{
+    int ret;
+
+    mutex_lock(&priv->phy_lock);
+    ret = __rtl837x_phys_write_c45(priv, phy_mask, devad, regnum, val);
+    mutex_unlock(&priv->phy_lock);
+
+    return ret;
+}
+
+int rtl837x_phy_write_c45(struct rtl837x_priv *priv, int phy,
+               int devad, int regnum, u16 val)
 {
-	return rtl837x_phys_write_c45(priv, BIT(phy), devad, regnum, val);
+    if (phy < 0 || phy >= priv->num_ports)
+        return -EINVAL;
+
+    return rtl837x_phys_write_c45(priv, BIT(phy), devad, regnum, val);
 }
 
 int rtl837x_phy_bits_read_c45(struct rtl837x_priv *priv, int phy, int devad, int regnum, u16 mask, u16 *pdata)
@@ -395,14 +428,18 @@ int rtl837x_phy_bits_write_c45(struct rt
 	int ret;
 	u16 val;
 
-	ret = rtl837x_phy_read_c45(priv, phy, devad, regnum, &val);
+	mutex_lock(&priv->phy_lock);
+	ret = __rtl837x_phy_read_c45(priv, phy, devad, regnum, &val);
 	if (ret)
-		return ret;
+		goto out_unlock;
 
 	val &= ~mask;
 	val |= (data << __ffs(mask)) & mask;
 
-	return rtl837x_phy_write_c45(priv, phy, devad, regnum, val);
+	ret = __rtl837x_phys_write_c45(priv, BIT(phy), devad, regnum, val);
+out_unlock:
+    mutex_unlock(&priv->phy_lock);
+    return ret;
 }
 
 int rtl837x_sds_reg_read(struct rtl837x_priv *priv, u8 sds_idx, u16 sds_page, u16 sds_reg, u16 *pdata)
--- a/src/rtl837x_mdio.c
+++ b/src/rtl837x_mdio.c
@@ -182,6 +182,7 @@ static int rtl837x_mdio_probe(struct mdi
 
 	mutex_init(&priv->map_lock);
 	mutex_init(&priv->ita_lock);
+	mutex_init(&priv->phy_lock);
 	
 	rc = rtl837x_mdio_regmap_config;
 	rc.lock_arg = priv;
